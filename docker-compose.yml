version: "3.9"
services:
  app:
    build: .
    env_file:
      - .env
    volumes:
      - ./.chroma:/app/.chroma
      - ./docs:/app/docs
    command: ["devmate", "I want to build a site showing nearby hiking trails"]
